node_modules/
dist/
.DS_Store
*.local

# Umgebungsvariablen — nur das Beispiel wird versioniert
.env
.env.*
!.env.example

# Vercel
.vercel

# Supabase CLI
supabase/.temp
supabase/.branches

# PostFinance Grotesk — lizenzpflichtig, liegt lokal, wird nicht verteilt.
# Bewusst hier: Ein öffentliches Repository wäre Weitergabe, und genau die
# verbietet die Lizenz. Lokal und im lokalen Build ist die Schrift aktiv.
# Wer sie trotzdem ausliefern will: git add -f public/fonts
public/fonts/

# Debug-Seite, gehört nicht in die Auslieferung (alles in public/ wird deployt).
public/_probe.html
