# Code

The runnable solution.

## Start

> One command, on a clean machine. Kept honest — this block is verified on a second laptop, not assumed.

```bash
# TODO: fill in as soon as the stack is chosen
```

**Requirements:** _TODO_
**Runs on:** http://localhost:_TODO_

## Tests

```bash
# TODO
```

## Structure

_TODO — one line per top-level directory, so a reviewer knows where to look first._

## Data

No real customer data. PostFinance provided no dataset and no API for this challenge, so all
transaction data in this repo is **synthetic and generated by us**. Generator: _TODO_.

## Notes

- Font: PostFinance Grotesk is **not** shipped (licensing). UI uses Inter 300/500/700.
